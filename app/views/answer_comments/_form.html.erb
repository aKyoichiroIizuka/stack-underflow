<%= form_for(@answer_comment) do |f| %>
  <% if @answer_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_comment.errors.count, "error") %> prohibited this answer_comment from being saved:</h2>

      <ul>
      <% @answer_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<p>
<%= f.label :content %><br />
<%= f.text_field :content, :size => 60 %>
</p>

<p>
<%= f.label :user_id %><br />
<%= f.select :user_id, User.all.collect {|x| [x.email, x.id]} %>
</p>

<p>
<%= f.label :answer_id %><br />
<%= f.select :answer_id, Answer.all.collect {|x| [x.content, x.id]} %>
</p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
